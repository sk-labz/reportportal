{
  "_comment": "EXTENDED DASHBOARDS ONLY (Release / Sprint Report). VERIFY before first run: the 'ex' (attribute key exists, regardless of value) condition for 'compositeAttribute' against POST /api/v1/{project}/filter on the target RP 5.15 instance -- if 'ex' isn't a supported condition, an alternative is 'has' with a wildcard-style value, or omitting this entity and instead relying on itemsCount alone. {{BUILD_ATTRIBUTE_KEY}} is substituted by provision_dashboards.py from each project's optional 'build_attribute_key' (default 'version') -- see config/teams.example.yml and ci-examples/ATTRIBUTES.md.",
  "name": "Recent Releases ({{BUILD_ATTRIBUTE_KEY}})",
  "description": "Most recent launches tagged with a '{{BUILD_ATTRIBUTE_KEY}}' attribute, used to scope Release/Sprint Report comparison widgets.",
  "type": "Launch",
  "entities": [
    {
      "filteringField": "compositeAttribute",
      "condition": "ex",
      "value": "{{BUILD_ATTRIBUTE_KEY}}"
    }
  ],
  "selectionParameters": {
    "orders": [
      { "sortingColumn": "startTime", "isAsc": false }
    ],
    "launchesLimit": 10
  }
}
