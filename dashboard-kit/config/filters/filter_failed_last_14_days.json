{
  "_comment": "CONFIRMED against a live RP 5.15.x instance (POST /v1/{project}/filter -> UpdateUserFilterRQ: top-level 'conditions'/'orders'). 'statistics$executions$failed' + 'gt' + '0' is accepted. Recency is bounded by widget itemsCount (e.g. last 50 launches) rather than an absolute startTime range, since RP saved filters store literal values, not rolling 'last N days' windows.",
  "name": "Launches With Failures (recent)",
  "description": "Recent launches that contain at least one failed test, used to scope failure/defect breakdown widgets.",
  "type": "Launch",
  "conditions": [
    {
      "filteringField": "statistics$executions$failed",
      "condition": "gt",
      "value": "0"
    }
  ],
  "orders": [
    { "sortingColumn": "startTime", "isAsc": false }
  ]
}
